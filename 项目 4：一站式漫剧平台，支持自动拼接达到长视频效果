# AI 漫剧一体化平台 PRD

## 1. 文档信息
- 产品名称：Manju（AI 漫剧一体化平台）
- 文档版本：v1.0
- 更新时间：2026-03-24
- 文档类型：用户侧与行业导向 PRD
- 当前阶段：MVP 已上线（分镜->图->视频->合并）

## 2. 行业背景与机会（漫剧）

### 2.1 行业现状
- 漫剧内容消费节奏快，热点迭代速度高，创作者面临“高频供给”压力。
- 传统制作链路（编剧、分镜、作画、动效、剪辑）环节多、协同成本高。
- 爆款不确定性高，团队需要低成本、快速试错。

### 2.2 核心痛点
- 单条漫剧从构想到成片周期过长，难以跟上平台热点。
- 素材资产分散，复用率低，重复劳动多。
- 人力依赖重，个人创作者难以独立完成完整生产链路。

### 2.3 机会判断
- AIGC 能将“创意->可发布内容”的周期从天级压缩到小时级/分钟级。
- 行业需要的是“生产工作台”，而不是单点模型能力。

## 3. 目标用户与场景

### 3.1 目标用户
- 个人漫剧创作者：追求快速产出与稳定更新。
- MCN 编导团队：需要并行生产与流程标准化。
- 中小内容工作室：希望降低前期人力与制作成本。

### 3.2 核心场景
- 热点跟进：在 1-3 小时内完成短漫剧视频上线。
- 系列连载：按统一风格批量产出多个分镜/视频。
- 商单内容：快速形成可交付草案与版本迭代。

### 3.3 JTBD
- 当我有一个剧情想法时，我希望一句话就能拆出可执行分镜，并快速生成可发布的漫剧视频，且能灵活调整顺序与版本。

## 4. 产品定位与价值主张

### 4.1 产品定位
- 面向漫剧创作者的 AI 一体化生产工作台。

### 4.2 价值主张
- 从“想法”到“可发布视频”的最短路径。
- 用户拥有最终决策权：分镜可编辑、图片/视频生成可控、顺序可调整。
- 资产可沉淀：图片与视频统一入库，支持后续复用与组合。

## 5. 产品目标与成功指标

### 5.1 北极星指标
- `Prompt -> 可发布成片` 中位时长（TTV, Time to Video）。

### 5.2 关键业务指标
- 分镜可用率（生成后无需重写即可进入出图）
- 分镜出图成功率
- 图生视频成功率
- 视频序列合并成功率
- 单项目平均迭代次数
- 7 日留存（创作者复访）

### 5.3 目标值（MVP->M1）
- TTV 中位时长 <= 20 分钟（3-5 镜）
- 出图成功率 >= 92%
- 出视频成功率 >= 88%
- 合并成功率 >= 95%

## 6. 功能需求（以用户价值为核心）

### 6.1 已实现（MVP）
1. 一句话生成分镜（1~5）
   - 固定结构：
     - 【角色】
     - 【场景】
     - 【动作/事件】
     - 【镜头语言】
     - 【情绪/氛围】
     - 【风格/画质】
   - 用户价值：降低前期创意结构化成本。

2. 分镜可编辑
   - 用户可逐镜改写文本后再生成素材。
   - 用户价值：确保内容决策权在创作者侧。

3. 一镜一图 + 一图一视频
   - 单个分镜对应单张图片，单张图片对应单条视频。
   - 用户价值：定位问题清晰，便于逐镜优化。

4. 视频顺序调整 + 一键合并
   - 在资源库选择视频、调整顺序、执行自动合并。
   - 用户价值：快速获得更长成片，无需手工剪辑。

5. 资源库（本地 SQLite）
   - 图片/视频资产统一管理，支持删除。
   - 用户价值：可复用、可管理、可追溯。

### 6.2 M1（下一阶段）
- 项目维度管理（项目、版本、标签）
- 分镜模板库（搞笑、悬疑、反转等）
- 批量重试机制（失败镜头重试）
- 导出策略（封面、标题、简介推荐）

## 7. 关键流程（用户视角）
1. 用户输入一句话剧情
2. 系统生成 1~5 个分镜
3. 用户编辑每个分镜
4. 逐镜生成图片
5. 逐镜生成视频（可选时长）
6. 选择多条视频 -> 调整顺序
7. 一键合并长视频
8. 在资源库管理/删除/复用

## 8. 模型与参数策略

### 8.1 模型配置
- 文本（分镜）：`deepseek/deepseek-v3.2-251201`
- 图片：`kling-v2`
- 视频：`kling-v2-6`
- Base URL：`https://api.qnaigc.com/v1`

### 8.2 视频参数策略（面向用户简化）
- 必填：`prompt`（分镜文本）
- 固定：`sound=on`
- 可选：`seconds=5|10`
- 固定：`input_reference` 单图
- 默认：`size` 使用系统默认分辨率

> 注：不同视频模型对“图生视频”能力与参数支持差异较大，切换模型前需做兼容性校验。

## 9. 非功能需求
- 稳定性：关键链路异常可定位（分镜/图片/视频/合并）
- 性能：MVP 单项目（3-5 镜）在可接受时长内完成
- 可观测性：记录任务状态、失败原因、重试入口
- 安全性：API Key 仅后端环境变量管理，不入前端与仓库
- 资产管理：图片/视频落本地 SQLite，支持删除

## 10. 商业化与增长假设
- 商业化方向：
  - 订阅分层（调用额度、并发量）
  - 团队版席位（协同与项目共享）
  - 高级模板与风格包
- 增长飞轮：
  - 模板复用 -> 产出更快 -> 更新更频繁 -> 数据反馈更好 -> 留存提升

## 11. 风险与应对
- 模型稳定性波动：增加失败重试和降级策略
- 画面风格一致性不足：引入风格模板与提示词规范
- 版权与合规风险：增加素材来源标记与审核流程
- 平台审核风险：增加负向词和安全规则策略

## 12. 里程碑
- P0（已完成）：分镜/图片/视频/合并/资源库
- P1（1-2 个迭代）：项目化管理 + 模板库 + 批量重试
- P2（可商用）：团队协作、计费体系、运营看板

